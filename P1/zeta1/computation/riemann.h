/**
 * Compute pi from riemann approx
 * @param dobule piApprox PI*PI/6
 * @return double approx. pi
 */
double piFromRiemann(double piApprox);

/**
 * Compute pi part of riemann approximation
 * @param unsigned long n -> part of riemann to compute
 * @return double part of pi 
 */
double piRiemannPart(unsigned long n);
